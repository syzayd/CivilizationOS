# Tested on Python 3.12-3.14. Minimum versions below are the ones verified to
# work on 3.14 (older pins like numpy 2.2 / pydantic 2.10 fail to import there).

# --- Web / API ---
fastapi>=0.139.0
uvicorn[standard]>=0.49.0
websockets>=16.0
pydantic>=2.13.4
pydantic-settings>=2.14.2
python-dotenv>=1.2.2

# --- Persistence ---
# Vector search is a small numpy-backed store (api/memory/vectorstore.py): no
# native build deps, fast enough at our scale, and keeps full control of TCMF scoring.
sqlmodel>=0.0.39
networkx>=3.4.2
numpy>=2.5.0

# --- LLM brains (3-tier router) ---
ollama>=0.6.2
google-generativeai>=0.8.3
anthropic>=0.42.0
httpx>=0.28.1

# --- ML / Phase 4 ---
mlflow>=2.20.2

# --- Dev / test ---
pytest>=8.3.4
pytest-asyncio>=0.25.0
