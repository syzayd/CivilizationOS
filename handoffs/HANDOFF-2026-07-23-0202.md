# HANDOFF - 2026-07-23 02:02 IST

## What this session did (TCMF paper)

1. **Wrote the full paper draft** (LaTeX, ~8pg) and an **adversarial self-review** (`REVIEW.md`,
   9 ranked gaps vs the correct neighbouring literature). Folded the fixable gaps into the draft
   (operator non-obviousness argument, close-related-work engagement, candidate-pool disclosure,
   hyperparameter-fairness, missing-vs-spurious edge limitation, anisotropy demoted).
2. **Made the paper private.** The draft was briefly pushed to the public CivilizationOS repo, then
   **removed from public history by force-push** and moved to a new **private repo `syzayd/tcmf-paper`**.
   It is checked out in place at `research/tcmf_paper/paper/`, which is **gitignored** inside
   CivilizationOS. Edit there; `git push` from that folder goes to the private repo.
3. **Built the decision-quality tier** (answers the biggest reviewer gap, W1): Sonnet subagent built
   `tcmfbench/decision.py`, `run_decision.py`, `llm_client.py`; Opus cross-verified and fixed two
   bugs (a `parse_letter` article-"a" misparse; oracle set-ordering nondeterminism), unit-tested the
   parser, ran smoke n=8 then full n=60.

## Key result (F8)
Each method's top-5 retrieved memories are shown to an LLM advisor (qwen2.5:3b-instruct, cached)
that picks the crisis root cause from a 4-way MCQ (true governance cause + 3 external-shock decoys).
n=60: decision accuracy TRACKS causal recall. Fixed retriever `tcmf_shipped` **0.97 ~= oracle 0.95**;
symptom-only (episodic 0.25, semantic 0.35) at the **no-retrieval floor 0.32**; old multiplicative
fusion 0.50. Favor-root weighting lifts tcmf_shipped (0.97) above tcmf_add (0.83) - the depth fix
matters for decisions, not just root_rank. **Retrieval choice changes the decision.**

## Current state
- **Both repos pushed and in sync.** CivilizationOS (public): benchmark code + `results_decision/` +
  `FINDINGS.md` F8 (commit `eafb445`, plus this handoff/log commit). `syzayd/tcmf-paper` (private):
  decision-tier Section + Table, updated abstract/contributions/limitations, `REVIEW.md` W1 marked
  addressed (commit `5970c50`).
- LaTeX validated structurally (balanced, 373/373 braces, no em dash, all 8 cites resolve, tables
  consistent). Not compiled here (no local TeX; build via Overleaf/MiKTeX).
- LLM answers cached in `results_decision/llm_cache.json` (590 entries) -> rerun is offline/exact.

## How to resume / reproduce
From `research/tcmf_paper/` with `..\..\.venv\Scripts\python` and `$env:PYTHONIOENCODING="utf-8"`:
- `python -m tcmfbench.run_decision --n 60 --out results_decision`  (needs Ollama + qwen2.5:3b-instruct; cached after first run)
- Paper: `cd paper` then edit `main.tex`; `git push` there -> private repo.

## Exact next steps (ranked, from REVIEW.md)
1. Verify every arXiv ID in `paper/references.bib` (never cite from memory) and cross-check the
   draft with a field expert - the last gate before any submission.
2. Cheap rigor adds: multi-seed + paired significance (Wilcoxon), held-out lambda/tau tuning split.
3. Spurious-edge robustness (inject false ancestors at rate p) - answers "your graph is assumed clean".
4. Larger candidate pool (>=40 distractors/noise) so recall@10 regains meaning; second encoder for
   the anisotropy-threshold generality; BFS-vs-plain latency numbers.

## Notes
- No `/wrap` full ritual was requested; this handoff + the master log + everything-we-did entry +
  the memory update ([[project-tcmf-paper]]) are the close. Laptop was shut down at end of session
  per Zaid's instruction.
